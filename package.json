{
  "name": "porfolio",
  "version": "1.0.0",
  "description": "Mi portfolio para trabajos",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /portfolio-juan",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "parcel": "^2.10.2"
  }
}
